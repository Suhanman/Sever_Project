apiVersion: v1
kind: Service
metadata:
  name: mariadb-headless
  namespace: database
  labels:
    app: mariadb
spec:
  clusterIP: None       # Headless: DNS로 Pod IP 직접 반환
  ports:
    - port: 3306
      name: mysql
  selector:
    app: mariadb
