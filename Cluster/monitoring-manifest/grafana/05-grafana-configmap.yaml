apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
  namespace: monitoring
data:
  grafana.ini: |
    [security]
    allow_embedding = true

    [auth.anonymous]
    enabled = true
    org_role = Viewer
